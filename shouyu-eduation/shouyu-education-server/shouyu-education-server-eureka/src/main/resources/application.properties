# profiles
spring.profiles.active=dev

# eureka
eureka.client.serviceUrl.defaultZone=http://${info.eureka.host}:${info.eureka.port}/eureka/
eureka.environment=${spring.profiles.active}
eureka.datacenter=shouyu-education

# application
spring.application.name=shouyu-education-server-eureka

# logback
logging.config=classpath:logback.xml
info.logging.path=./logs/server-eureka

# info
info.build.name=@project.name@
info.build.description=@project.description@
info.build.groupId=@project.groupId@
info.build.artifact=@project.artifactId@
info.build.version=@project.version@
